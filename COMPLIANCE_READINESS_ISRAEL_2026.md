# InvoiceFlow Hebrew — מוכנות לרישום תוכנת הנהלת חשבונות בישראל

## חשוב
המסמך הזה הוא checklist הנדסי לקראת רישום התוכנה. רשות המסים רושמת תוכנות במרשם; הרישום אינו תחליף לבדיקה מקצועית/משפטית או לקבלת תעודת רישום בפועל.

## דרישות שנבדקו מול מקורות רשות המסים

1. **רישום תוכנה** — תוכנה לניהול מערכת חשבונות ממוחשבת שחייבת ברישום צריכה להירשם במרשם התוכנות של רשות המסים.
2. **מודול קובץ במבנה אחיד** — תוכנה החייבת ברישום צריכה לאפשר הפקת קבצים בהתאם להוראות "קובץ במבנה אחיד". יש להשתמש במהדורה העדכנית של רשות המסים בעת ההגשה.
3. **רצף מספור** — יש למנוע שינוי של מספר מסמך לאחר הפקה ולבקר רצף מספרים.
4. **מסמך קבוע** — מסמך שהופק לא ניתן לעריכה או מחיקה; תיקון צריך להתבצע באמצעות מנגנון מסמך מתאים, כגון זיכוי/ביטול בהתאם לנסיבות.
5. **איתור מסמכים** — יש יכולת לאתר מסמכים שהופקו ולשמור את הקשר ביניהם.
6. **פלט ובקרה** — נדרשים פלטים ניתנים לביקורת, כולל נתוני העסק, התקופה, מהות הפלט, תאריך הפקה, מספור עמודים וסימון מתאים לפלט זמני/קבוע לפי הדרישות החלות.
7. **גיבוי** — נדרש מנגנון גיבוי יזום/מבוקר בהתאם להוראות. אחסון localStorage בדפדפן לבדו אינו פתרון מספק למערכת ייצור המיועדת לרישום.
8. **חשבוניות ישראל** — נכון ל-2026, חובת מספר הקצאה חלה על חשבוניות מס רלוונטיות מעל 5,000 ₪ לפני מע"מ, החל מ-1 ביוני 2026. המערכת צריכה להתחבר לשירותי רשות המסים ולהחזיר את מספר ההקצאה לפני מסירת החשבונית כאשר הוא נדרש.
9. **בדיקת קובץ** — רשות המסים מפעילה סימולטור לבדיקת תקינות ראשונית של קובץ במבנה אחיד לפני הגשת הרישום.

## מה כבר הוטמע בענף `compliance-readiness-2026`

- מודל מסמכים עם סוגים: חשבונית מס, קבלה, חשבונית זיכוי.
- סטטוס מסמך מפורט: טיוטה / הופק / נשלח / שולם / מבוטל.
- מסמך שהופק נעול לעריכה.
- מסמך שהופק אינו ניתן למחיקה.
- ביטול מסמך נשמר עם סיבה ותאריך במקום מחיקה.
- הוספת אירועי audit למסמך בעת הפקה, שינוי סטטוס וביטול.
- שמירת מזהה שלמות למסמך בעת ההפקה.
- קשר בין חשבונית זיכוי למסמך המקורי.
- חסימה של הפקת חשבונית מס/זיכוי לעוסק פטור.
- בדיקות תקינות של כמות ומחיר.
- סימון חשבונית מס הדורשת מספר הקצאה לפי סף 2026.
- הצגת מספר הקצאה במסמך אם התקבל.
- הגדרות עסק מורחבות: סוג עוסק, שיעור מע"מ, סדרת מסמכים.

## חסמים שחייבים להשלים לפני הגשת בקשה

### P0 — חובה

- [ ] להעביר את מקור האמת של המסמכים מה-localStorage למסד נתונים/אחסון שרת מבוקר.
- [ ] לממש מנגנון מספור אטומי בצד שרת כדי למנוע כפילויות בתרחיש של שני משתמשים במקביל.
- [ ] לממש מנגנון קובץ במבנה אחיד לפי **המהדורה העדכנית ביום ההגשה**, כולל כל הרשומות והשדות הנדרשים ולא רק export כללי.
- [ ] לבדוק את הקובץ בסימולטור הרשמי של רשות המסים ולשמור את פלט הבדיקה כחלק מחבילת ההגשה.
- [ ] לממש חיבור אמיתי ל-API של חשבוניות ישראל / שירותי רשות המסים, כולל הרשאה, טיפול בטוקנים, טיפול בשגיאות ושמירת מספרי הקצאה.
- [ ] להבטיח שלא ניתן להפיק/לשלוח חשבונית החייבת במספר הקצאה ללא מספר הקצאה תקף.
- [ ] לממש גיבוי אוטומטי ויכולת שחזור, כולל בדיקות שחזור מתועדות.
- [ ] להוסיף הרשאות משתמשים בצד שרת ולוג פעולות שאינו ניתן לשינוי על ידי משתמש רגיל.
- [ ] להוסיף מנגנון פלט קבוע/מודפס מלא לצורכי ביקורת, לפי הוראות ניהול פנקסים והדרישות הטכניות העדכניות.
- [ ] להכין ספרות הדרכה מקצועית למערכת וחבילת הגשה בהתאם לטופס/הליך הרישום של רשות המסים.

### P1 — חשוב

- [ ] חתימת גרסה/זיהוי גרסה של התוכנה והמהדורה שנרשמת.
- [ ] תיעוד שינויים בין מהדורות.
- [ ] מנגנון שחזור נתונים ובדיקת תקינות ארכיון.
- [ ] בדיקות אוטומטיות למספור, זיכויים, ביטולים, מע"מ, מספרי הקצאה וייצוא מבנה אחיד.
- [ ] תהליך release שמבטיח שגרסה רשומה אינה משתנה ללא תהליך גרסה מסודר.

## מקורות רשמיים

- רשות המסים — בקשה לרישום תוכנה לניהול מערכת חשבונות ממוחשבת.
- רשות המסים — הוראות ניהול ספרים בסביבה ממוחשבת.
- רשות המסים — הוראות ליצרני תוכנות וקובץ במבנה אחיד.
- רשות המסים — סימולטור לבדיקת קובץ במבנה אחיד.
- רשות המסים — חשבוניות ישראל ומספרי הקצאה.

יש לאמת מחדש את המהדורות, הספים, כתובות ה-API והטפסים ביום ההגשה בפועל, משום שהוראות רשות המסים מתעדכנות.
