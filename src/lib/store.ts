import { useEffect, useState } from "react";
import { supabase } from "@/integrations/supabase/client";

export type DocType = "invoice" | "receipt" | "credit_note";
export type DocStatus = "draft" | "issued" | "sent" | "paid" | "cancelled";
export type BusinessRole = "owner" | "admin" | "user";

export type Client = { id:string; name:string; taxId?:string; email?:string; phone?:string; address?:string; isVatRegistered:boolean; };
export type LineItem = { id:string; description:string; quantity:number; unitPrice:number; };
export type BusinessInfo = { id:string; name:string; taxId:string; address:string; phone:string; email:string; documentPrefix:string; businessType:"osek_patur"|"osek_murshe"|"company"; vatRate:number; };
export type Doc = { id:string; type:DocType; number:string; clientId:string; issueDate:string; dueDate:string; items:LineItem[]; vatRate:number; status:DocStatus; notes?:string; paymentMethod?:string; relatedDocumentId?:string; allocationRequested:boolean; allocationNumber?:string; allocationRequestedAt?:string; subtotal?:number; vatAmount?:number; totalAmount?:number; contentHash?:string; };
export type AppData = { business:BusinessInfo; businessId:string; role:BusinessRole; clients:Client[]; docs:Doc[]; };
export const uid=()=>Math.random().toString(36).slice(2,10); const today=()=>new Date().toISOString().slice(0,10); const plusDays=(n:number)=>new Date(Date.now()+n*86400000).toISOString().slice(0,10);
let cache:AppData|null=null; let inflight:Promise<AppData|null>|null=null; const listeners=new Set<()=>void>(); const notify=()=>listeners.forEach(l=>l()); type UnknownRecord=Record<string,unknown>;
async function resolveActiveBusiness(userId:string){const {data:memberships,error}=await supabase.from("business_members").select("business_id, role").eq("user_id",userId).order("created_at",{ascending:true}); if(error)throw error; if(memberships&&memberships.length){const stored=typeof window!=="undefined"?window.localStorage.getItem("active-business"):null;const chosen=memberships.find(m=>m.business_id===stored)??memberships[0]!;return {businessId:chosen.business_id,role:chosen.role as BusinessRole};} const {data:profile}=await supabase.from("profiles").select("business_name, tax_id, business_type, email").eq("id",userId).maybeSingle(); const {data:newId,error:rpcError}=await supabase.rpc("create_business",{_name:profile?.business_name||"העסק שלי",_tax_id:profile?.tax_id??"",_business_type:profile?.business_type??"osek_patur",_email:profile?.email??""}); if(rpcError)throw rpcError; return {businessId:newId as string,role:"owner" as BusinessRole};}
async function fetchAll():Promise<AppData|null>{const {data:auth}=await supabase.auth.getUser();const user=auth.user;if(!user)return null;const {businessId,role}=await resolveActiveBusiness(user.id);const [businessRes,clientsRes,docsRes]=await Promise.all([supabase.from("businesses").select("*").eq("id",businessId).single(),supabase.from("clients").select("*").eq("business_id",businessId).order("created_at",{ascending:true}),supabase.from("documents").select("*, document_items(*)").eq("business_id",businessId).order("created_at",{ascending:false})]);if(businessRes.error)throw businessRes.error;if(clientsRes.error)throw clientsRes.error;if(docsRes.error)throw docsRes.error;const b=businessRes.data;return {businessId,role,business:{id:b.id,name:b.name,taxId:b.tax_id,address:b.address,phone:b.phone,email:b.email,documentPrefix:b.document_prefix,businessType:b.business_type,vatRate:Number(b.vat_rate??18)},clients:(clientsRes.data??[]).map(c=>{const row=c as UnknownRecord;return{id:c.id,name:c.name,taxId:c.tax_id,email:c.email,phone:c.phone,address:c.address,isVatRegistered:Boolean(row.is_vat_registered)}}),docs:(docsRes.data??[]).map(d=>{const row=d as UnknownRecord;return{id:d.id,type:d.type as DocType,number:d.number,clientId:d.client_id,issueDate:d.issue_date,dueDate:d.due_date,vatRate:Number(d.vat_rate),status:d.status as DocStatus,notes:d.notes,paymentMethod:d.payment_method,relatedDocumentId:typeof row.related_document_id==="string"?row.related_document_id:undefined,allocationRequested:Boolean(row.allocation_requested),allocationNumber:typeof row.allocation_number==="string"?row.allocation_number:undefined,allocationRequestedAt:typeof row.allocation_requested_at==="string"?row.allocation_requested_at:undefined,subtotal:typeof row.subtotal==="number"?row.subtotal:undefined,vatAmount:typeof row.vat_amount==="number"?row.vat_amount:undefined,totalAmount:typeof row.total_amount==="number"?row.total_amount:undefined,contentHash:typeof row.content_hash==="string"?row.content_hash:undefined,items:[...((d.document_items??[]) as Array<Record<string,unknown>>)].sort((x,y)=>Number(x.position)-Number(y.position)).map(i=>({id:String(i.id),description:String(i.description??""),quantity:Number(i.quantity),unitPrice:Number(i.unit_price)}))};})};}
export async function refresh(){if(!inflight){inflight=fetchAll().finally(()=>{inflight=null;});}cache=await inflight;notify();return cache;}
export function useData(){const[state,setState]=useState<AppData|null>(cache);useEffect(()=>{const sync=()=>setState(cache?{...cache}:null);listeners.add(sync);void refresh();return()=>{listeners.delete(sync);};},[]);return state;}
function requireBusiness(){if(!cache)throw new Error("NO_ACTIVE_BUSINESS");return cache.businessId;}
export const actions={async saveBusiness(business:BusinessInfo){const businessId=requireBusiness();const{error}=await supabase.from("businesses").update({name:business.name,tax_id:business.taxId,address:business.address,phone:business.phone,email:business.email,document_prefix:business.documentPrefix??""}).eq("id",businessId);if(error)throw error;await refresh();},async saveClient(client:Client){const business_id=requireBusiness();const payload={business_id,name:client.name,tax_id:client.taxId??"",email:client.email??"",phone:client.phone??"",address:client.address??"",is_vat_registered:client.isVatRegistered};const exists=cache?.clients.some(c=>c.id===client.id);const query=exists?supabase.from("clients").update(payload as never).eq("id",client.id):supabase.from("clients").insert(payload as never);const{error}=await query;if(error)throw error;await refresh();},async deleteClient(id:string){const{error}=await supabase.from("clients").delete().eq("id",id);if(error)throw error;await refresh();},async saveDoc(doc:Doc):Promise<string>{const business_id=requireBusiness();const base={business_id,client_id:doc.clientId,type:doc.type,issue_date:doc.issueDate,due_date:doc.dueDate,vat_rate:doc.vatRate,notes:doc.notes??"",payment_method:doc.paymentMethod??"",related_document_id:doc.relatedDocumentId??null,allocation_requested:doc.allocationRequested,allocation_number:doc.allocationNumber??null,allocation_requested_at:doc.allocationRequested?doc.allocationRequestedAt??new Date().toISOString():null};const exists=Boolean(doc.id)&&cache?.docs.some(d=>d.id===doc.id);let docId=doc.id;if(exists){const{error}=await supabase.from("documents").update(base as never).eq("id",doc.id);if(error)throw error;const{error:delErr}=await supabase.from("document_items").delete().eq("document_id",doc.id);if(delErr)throw delErr;}else{const{data,error}=await supabase.from("documents").insert(base as never).select("id").single();if(error)throw error;docId=data.id;}const items=doc.items.filter(i=>i.description.trim()).map((i,index)=>({document_id:docId,description:i.description,quantity:i.quantity,unit_price:i.unitPrice,position:index}));if(items.length){const{error}=await supabase.from("document_items").insert(items);if(error)throw error;}await refresh();return docId;},async issueDoc(id:string){const{error}=await supabase.rpc("issue_document",{_document_id:id});if(error)throw error;await refresh();},async deleteDoc(id:string){const doc=cache?.docs.find(d=>d.id===id);if(doc&&doc.status!=="draft")throw new Error("ISSUED_DOCUMENT_CANNOT_BE_DELETED");const{error}=await supabase.from("documents").delete().eq("id",id);if(error)throw error;await refresh();},async cancelDoc(id:string,reason=""){const{error}=await supabase.rpc("cancel_document",{_document_id:id,_reason:reason});if(error)throw error;await refresh();},async setStatus(id:string,status:DocStatus){if(status==="cancelled")return actions.cancelDoc(id);if(!["sent","paid"].includes(status))throw new Error("INVALID_STATUS_TRANSITION");const{error}=await supabase.from("documents").update({status}).eq("id",id);if(error)throw error;await refresh();}};
export function emptyDoc(type:DocType):Doc{return{id:"",type,number:"",clientId:"",issueDate:today(),dueDate:plusDays(30),items:[{id:uid(),description:"",quantity:1,unitPrice:0}],vatRate:18,status:"draft",allocationRequested:false};}
export function totals(doc:Pick<Doc,"items"|"vatRate">){const subtotal=doc.items.reduce((s,i)=>s+i.quantity*i.unitPrice,0);const vat=subtotal*doc.vatRate/100;return{subtotal,vat,total:subtotal+vat};}
export const money=(n:number)=>new Intl.NumberFormat("he-IL",{style:"currency",currency:"ILS",maximumFractionDigits:2}).format(n||0);export const dateHe=(iso:string)=>iso?new Intl.DateTimeFormat("he-IL").format(new Date(iso)):"";export const statusLabel:Record<DocStatus,string>={draft:"טיוטה",issued:"הופק",sent:"נשלח",paid:"שולם",cancelled:"מבוטל"};export const typeLabel:Record<DocType,string>={invoice:"חשבונית מס",receipt:"קבלה",credit_note:"חשבונית זיכוי"};export const isLocked=(doc:Pick<Doc,"status">)=>doc.status!=="draft";
